# Home Automation — To-Do List

*Last updated: 2026-09-08 (weekly-disk-health)*

> **Structure rule (2026-07-28):** sections in order — Pending → Watching/deferred → Future → Completed (newest first, max ~10; older items go to `archive/TODO-completed.md` in this repo). **Scheduled-task appends must be ≤300 chars + a pointer** to the full report/TROUBLESHOOTING entry — no inline essays.

---

## Pending

- [ ] **Frigate records to `/srv/frigate` — woodhull's boot NVMe, not the 3.7 T `/srv/media`.** 44 GB after 5 days, still filling (14-day tier cycles ~09-17). Same shape as the 07-27 crash. Relocate or document. See infra/disk_reports/disk-health-2026-09-08.md — (via weekly-disk-health 2026-09-08)

- [ ] **Watch/bump frigate on woodhull — riding its own 2 GiB cap.** Since the 09-03 cutover frigate averages 83.8% / peaks 94.7% of 2 GiB (both cameras here now). No host risk (woodhull has ~9 GB free) but close to its own cap → frigate-only OOM risk. Bump `mem_limit` 2→3 GiB on woodhull, or add an OOM ntfy alert. See budget-report-2026-09-07.md — (via weekly-resource-budget 2026-09-07)

- [ ] **🟡 Rebuild `voice-bench` on woodhull with a new `config_tag` — it has been dark since 2026-09-03 and its LaunchAgent was retired 09-04.** Paul noticed it missing the same day; worth saying plainly that Phase 4 took it down deliberately (plan §2.1 "park it"), it wasn't an accident, but the user-visible loss wasn't flagged at the time.

  **Where it was:** native macOS LaunchAgent `com.voice-bench` on the Mac, dashboard at **`http://192.168.1.70:7700`** — plain HTTP, and `.70`, never `.71`. Booted out + `launchctl disable`d 2026-09-04. Nothing listens on `:7700` on either host now.

  **Why it was parked rather than moved:** every row it writes is stamped with `tag:`, currently `whisperkit-small-esphome-direct` — the exact pipeline this migration destroyed (WhisperKit/ANE → `wyoming-faster-whisper` container on woodhull). Moving a harness that had already been dead 08-07→08-16 through a platform change would have muddied the STT bake-off, which had its own measurement. Last data row: **2026-09-02 21:36**, the evening before cutover. `data/voice_bench.csv` is intact and is the historical baseline for the old pipeline — do not delete it, and do not append new-pipeline rows under the old tag.

  **Spec to rebuild:**
  1. **Port as a systemd user unit on woodhull**, same pattern as `go-librespot-naboo` / `naboo-stream` — including `loginctl enable-linger` and a real reboot to prove it, since that is the check launchd failed five times silently.
  2. It is plain Python: `run.sh` builds a venv from `requirements.txt` (`websockets`, `aiohttp`, `pyyaml`) and runs a daemon. No macOS dependency in the runtime itself.
  3. **`config.yaml` is half-migrated already** (`894ea91`): `ha_websocket_url` is correctly `ws://192.168.1.71:8123/api/websocket`, but `tag:` and `notes:` still describe WhisperKit + Silero-VAD 450 ms. Set a new tag — e.g. `fasterwhisper-base.en-esphome-direct` — and rewrite `notes:` to match what actually runs. **Comparing new rows against WhisperKit-tagged rows is misleading**, which is the whole reason for the tag field.
  4. `port: 7700` can stay; it becomes `http://192.168.1.71:7700`. Bind on `${HOST_IP}`, not `0.0.0.0`, per the house convention.
  5. **Fix the durable-restart bug first** — `home-automation/TODO.md` has carried "harness fragile, durable fix undeployed" since August. Rebuilding it on a new host without that fix just relocates the fragility.
  6. Add a Kuma monitor for `:7700` once it is up — it had none, which is part of why nobody noticed the 9-day August outage.
  7. `log-tailer-native` no longer tails `voice-bench--*` on the Mac (removed 09-04, the source was dead); woodhull's Docker `log-tailer` won't see a systemd unit either, so decide how its log gets into `infra/log-reports/`.

  **Timing:** good first task for the week between the stress test and the M1 leaving (~09-11) — it would give real voice-latency history from woodhull *before* the Mac goes, rather than after. The known open Voice-PE listening-phase bug (7+ weeks, `voice_abort_pipeline_if_listening_phase_exceeds_15_seconds`) will still show up as `total_ms` outliers; that is not an STT regression — compare STT hop times, not `total_ms`. — (via Cowork 2026-09-04)

- [ ] **🔵 Home-automation migration — Phase 3/4/5 remain. §10 #18 reboot survival: ✅ PASS 2026-09-04 13:42 ET.** Cold `sudo reboot` on woodhull: all 24 containers back and healthy at 2 min; `go-librespot-naboo` + `naboo-stream` both `active` (enable-linger holds — the launchd equivalent failed this 5x silently); detector `inference_speed` 8.39 ms, in the bake-off band; `:8765` served 122 KB in a 5 s pull. Spotify Connect device-picker confirmed by Paul — **#18 fully closed.** Phase 4 file-side edits done + committed 2026-09-04 (crontab.txt, tail-native.sh, watchdog.sh sentinel, infra/CONTEXT.md); **Mac-side launchctl/`crontab -e`/Login Items steps still need a shell on the Mac.** **Phase 3 + Phase 4 EXECUTED 2026-09-04 14:01 ET — both closed** (via `infra/scripts/phase34-mac.sh` + `phase34-mac2.sh`, run by Paul; Cowork has no route to the Mac). Zero containers on the Mac, guard fails by name, 6 agents booted out AND disabled, crontab applied, tail-native down to 3 sources, `BK_HA=0` added. Four landmines not in the plan — incl. the tailer watchdog's sentinel pointing at a file the same change deletes. **Still open: (a) FrigateDetector Login Item removal, GUI-only; (b) two stale-`enabled` labels `com.librespot.naboo` / `com.go-librespot.naboo-test`; (c) `unifi-syslog` looks dead and predates this — own item.** **Phase 5 (doc sweep + scheduled-task audit) DONE 2026-09-04** — 10 docs corrected (root CLAUDE.md, SYSTEM_CONTEXT incl. the whole port map, both hosts/ files, home-automation CONTEXT+CLAUDE, OPS_RUNBOOK, NP/RESILIENCE/DOWNSTAIRS plans, TROUBLESHOOTING); 16 scheduled tasks audited — 1 was generating false pages (`docker-critical-alert-check` flagged the Mac's deliberately-stopped containers every 15 min), 3 are broken pending rewrite, 7 premise-corrected, 4 fine. ⚠️ **Real gap found: front_window RTSP is unmonitored on either host** — camera-monitor retired on the Mac, never ported to woodhull. ⚠️ **Scheduled-task edits must be mirrored to `~/Documents/Claude/Scheduled/` or they do nothing.** Full detail: plan §7/8 and §9 RESULT. Full result: plan §10 #18 RESULT. — (via Cowork 2026-09-04)


- [ ] **🔴 ACTIVE — Naboo Spotify Connect / play_media investigation. READ THIS FIRST if starting a new session on this.** Long multi-session investigation, not yet fully resolved. Full blow-by-blow history is below this block (many superseded theories, kept for the record) — this is the current, consolidated state as of 2026-09-04 (Cowork).

  **FIXED and verified this session:**
  - Kuma unreachable via Tailscale (missing `tailscale serve` mapping) — fixed, see infra repo.
  - HA 400 Bad Request via Tailscale (`trusted_proxies` storage-migration lockout) — fixed, see infra repo.
  - `go-event.py` webhook `HA_URL` pointed at the retired Mac IP — fixed and confirmed working (both play/stop automations fire correctly on real Spotify Connect state changes).
  - **Race condition in `voice_resume_media_on_done`** (`automations.yaml`): it used to check `input_boolean.voice_paused_music` immediately on trigger, racing `StopMusic`'s intent script (which explicitly clears that same flag first, specifically to prevent an auto-resume after "stop" — see the comment already in `configuration.yaml`'s `StopMusic` action). Logs showed the resume automation winning that race by ~45ms, silently undoing real "stop" voice commands a few seconds later. **Fixed**: moved the automation's existing 1s delay to run *before* the condition checks instead of only before `media_play`, giving `StopMusic` a full second of margin. Committed (`2790861`), YAML validated, reloaded live via the HA API (no restart). **Not yet re-tested by Paul** — next session should confirm saying "stop" to Naboo mid-playback now actually stays stopped.

  **Ruled out (confirmed NOT the cause), don't re-chase these:**
  - `input_boolean.radio_active` getting stuck — red herring, self-clears normally.
  - Spotify DRM / `media is restricted` errors — real, but proven non-blocking (a clean album retest still failed the same way with zero such errors).
  - ESPHome API connection health — confirmed fully healthy; Paul toggled light/mic-mute/wake-sound switches from Developer Tools and Naboo responded instantly every time.
  - A stale dual-HA-client conflict from the Mac migration (found in logs, real, but historical — Mac's `homeassistant` container is confirmed `Exited`/stopped and not competing right now).
  - "Self-triggered false wake word" — this was an intermediate theory that turned out to be wrong; the `listening` events are real voice interactions (see the race-condition fix above), not acoustic false-positives. Don't resurrect this one without new evidence.

  **STILL OPEN — the real unresolved mystery:**
  1. **Naboo's physical device has never once made a real HTTP connection to fetch the custom Spotify relay stream** (`http://192.168.1.71:8765`, served by `librespot/serve_http.py`), across the *entire* multi-day investigation, confirmed as recently as the last test of this session (~23:30 ET). Checked via `stream.log`'s `CLIENT_CONNECT` lines (only ever from `192.168.1.71`, i.e. this session's own `curl` tests, never from Naboo's real IP `192.168.1.47`) and cross-checked live with `ss -tn | grep 8765` (zero established connections). HA-side, `media_player` reliably reports `playing` almost immediately after `play_media` is called — this is presumably optimistic/immediate on the ESPHome integration side and does **not** mean the device is actually fetching anything. **This is the one thing that actually matters and hasn't been explained.** The ESPHome device's own native log (Settings → Devices & services → the Naboo device → Logs, or via the "..." menu — note: no "Enable debug logging" option was found on this HA version's device page, worth checking the ESPHome integration's own page instead) is the one piece of evidence never obtained — browser automation was attempted twice this session and blocked both times by a flaky site-access approval. **Best next step:** either get that device Logs page (screenshot from Paul, or retry browser automation with a fresh approval), or test a hypothesis directly — try `media_player.play_media` with a URL shaped more like a normal stream (e.g. ending in `.mp3`, or serving explicit `Content-Length`) against our own server, to see if something about the URL/response shape is why ESPHome's media_player silently never attempts the fetch. (Radio Browser stations — real external stream servers — DID reach `playing` state in HA when tested, but whether they made real *sound* was never independently confirmed either, since that test got cut short by the (now-fixed) wake-word race.)
  2. **Spotify Connect track-skipping, most recent occurrence 2026-09-04 ~23:30 ET, still unexplained.** `librespot.log` showed 3 tracks loaded within 7 seconds (2-5s each), **zero errors logged** (ruling out the known "media is restricted" DRM case) — looks like literal skip-forward commands, not a decode/DRM failure. Could be related to #1 above (nothing ever really consumes the stream, so something in go-librespot's own buffering logic gives up and advances) or could simply be manual skips on Paul's phone during testing — **this question was asked of Paul but not yet answered when this session ended.** Ask again at the start of a new session before re-investigating.
  3. **Known, minor, not yet fixed: `serve_http.py`'s `THROUGHPUT` log line is misleading.** It counts bytes read from the ffmpeg FIFO unconditionally, not gated on whether an HTTP client is actually connected (`if client:` only gates the `sendall()`, not the byte counter below it) — this caused a real, hours-long false lead earlier in this investigation (mistook Spotify actively playing, with nobody fetching the stream, for "sustained client connection"). Low-priority cosmetic/diagnostic fix: gate `_tp_bytes` accounting on `if client:` too.

  **Key facts for a fresh session:**
  - Naboo = ESPHome entity prefix `0a3a76`, physical IP `192.168.1.47`. HA and all containers now live on `woodhull` (`192.168.1.71`), migration from the old Mac (`192.168.1.70`) is otherwise complete and verified.
  - Stream relay: `~/containers/home-automation/librespot/serve_http.py` (port 8765), log `librespot/log/stream.log`. go-librespot: `librespot/log/librespot.log`, local control API on `:3678`. `go-event.py` polls that API and posts webhooks to HA; its log is `librespot/log/events.log`.
  - Relevant automations, all in `~/containers/home-automation/homeassistant/automations.yaml`: `librespot_playing`/`librespot_stopped` (webhook-triggered stream attach/detach), `voice_pause_media_on_wake`, `voice_unmute_after_listening`, `voice_resume_media_on_done` (just fixed), `voice_premute_when_idle`, `naboo_unmute_on_play`. `StopMusic` intent script is in `configuration.yaml` (~line 359).
  - **Use HA's recorder DB for live/historical entity state** (`docker exec homeassistant python3` against `/config/home-assistant_v2.db`, tables `states`/`states_meta`), never `.storage/core.restore_state` (a periodic snapshot that caused a real diagnostic mistake earlier in this investigation — see TROUBLESHOOTING.md 2026-09-03 "SUPERSEDED" entries for what that cost).
  - `docker logs homeassistant --since <timestamp>` is unreliable on this container (silently misses real lines within the window) — use `--tail N` and grep instead, cross-check against DB timestamps.
  - An HA long-lived token is available at `~/containers/home-automation/secrets/ha.env` (`HA_TOKEN`) for direct API calls, e.g. `curl -H "Authorization: Bearer $HA_TOKEN" http://192.168.1.71:8123/api/services/...` — **use `192.168.1.71`, not `localhost`**, from woodhull's own shell (localhost doesn't reach the container's published port from the host in this setup).
  - `homeassistant/` is gitignored at the repo root except for specific tracked files (`automations.yaml`, `configuration.yaml`, `custom_sentences/`); `git add` on those can warn about the ignore pattern even though the file is already tracked — use `git add -f` if a plain `git add` seems to silently no-op.
  - All git operations go through the `wood`/`mac` SSH wrapper functions in this environment, never a device-bridge tool.
  — (via Cowork 2026-09-04, handoff written for a fresh session)


- [ ] **Evaluate HA 2026.8 upgrade** (on 2026.7). 2026.8 stable since 08-05 (templates ~40% faster; entities keep state on restart). Verify HACS base.py timeout + WindmillAC blynk_service.py patches survive first. UniFi Protect 7.1 change N/A (Frigate). — (via homelab-advisor 2026-09-02)
- [x] **Naboo voice→Spotify silently no-ops when Naboo has dropped out of Spotify's device list — ✅ FIXED 2026-09-01 (Cowork).** `spotify_voice_assistant.play()` used to fall back to a deviceless `start_playback` (targets "currently active device", i.e. nothing) whenever `get_devices()` didn't list Naboo — which happened any time it'd been idle since its last Zeroconf handshake. Happened 3x in <24h (08-31 21:19 & 21:25 ET, 09-01 17:26 ET "play the breeders") before the fix. **Fix:** `play_on_device()` now tries go-librespot's own local control API first for Naboo (`POST host.docker.internal:3678/player/play`) — it already holds valid Spotify credentials and self-initiates a session, bypassing the cloud device-list dependency entirely. Cloud path kept as fallback. Verified end-to-end post-fix: local-API log line fired, `stream.log` showed continuous audio throughput, ESPHome voice player entity confirmed `playing`/unmuted. See TROUBLESHOOTING.md 2026-09-01. — (via Cowork 2026-09-01)

- [ ] **`infra/log-reports/go-librespot*.log` stale since the 08-31 infra migration (log-tailer moved to woodhull, lost its mount into the Mac-local librespot log dir).** Use `home-automation/librespot/log/{librespot,stream,events,watchdog,run}.log` directly until re-wired (candidate: extend `log-tailer-native` on the Mac, same pattern already used for homeassistant/frigate/mosquitto). See TROUBLESHOOTING.md 2026-09-01. — (via Cowork 2026-09-01)

- [x] **Watch frigate memory creeping up on the Mac** — ~820→1444 MB (94% of 1.5 GiB) over 08-27→08-30, 48h avg ~80%. Lines up with the media stack leaving the Mac (08-26) freeing RAM (likely buffers, not a leak). If sustained >1.45 GB or OOM-restarts, investigate/bump cap. See budget-report-2026-08-31.md — (via weekly-resource-budget 2026-08-31) — carried over (via homelab-advisor 2026-09-02): still elevated — avg 78.9% / peak 96.0% of 1.5 GiB, live 93.9%, rising; no OOM (Up 2 d). Add an OOM/restart ntfy alert or bump the cap 1.5→2 GiB (Mac has ample RAM now). See advisor-2026-09-02.html — ✅ ACTED 2026-09-02: live-bumped the RUNNING Mac frigate to 2 GiB via `docker update --memory 2048m` (no restart, StartedAt unchanged; usage 93.9%→64.8%). NOTE: the on-disk compose is woodhull-staged (2048m + /dev/dri OpenVINO) and is NOT runnable on the Mac — do not `compose up` frigate on media; the 2 GiB lands natively at the woodhull cutover.

- [ ] **🔵 Migrate the home-automation stack (`homeassistant`, `mosquitto`, `frigate`) from `media` to `woodhull`.** Decided 2026-08-30: runs right after the infra-stack migration, same session. Frigate's detector changes from the Apple Silicon ANE bridge to OpenVINO on `woodhull`'s iGPU — not a straight port, needs its own verification. WhisperKit STT and go-librespot are native-macOS and out of scope for this move (WhisperKit needs a Linux Whisper port separately; librespot runs on Linux as-is once it's addressed). **PLAN WRITTEN 2026-08-31 (Cowork, planning only, uncommitted): `plans/HOME_AUTOMATION_STACK_MIGRATION.md`** — full pre-flight, two bake-offs (faster-whisper STT and the OpenVINO detector) run on woodhull *before* cutover, phase order, verification matrix, rollback. Decisions taken: full 28 GB Frigate rsync, recordings to `/srv/frigate` on the NVMe, STT replaced by `wyoming-faster-whisper` (no Mac fallback), go-librespot ported as systemd user units. The Stream Deck is NOT migrated — see `plans/DOWNSTAIRS_CONTROL_PLAN.md`. See also `plans/NP_HOMELAB_PLAN.md` §5 Phase A. EXECUTION STARTED 2026-08-31: Phase 1 prep done on woodhull (repo cloned, secrets/mosquitto/go-librespot config copied, compose rewritten, whisper container up, 28GB Frigate pre-seeded). Frigate/OpenVINO bake-off PASSED (7-9ms inference). STT bake-off blocked on Paul switching the live Mac HA's Wyoming integration and a 2-3 day real-use trial -- Phase 2 cutover does not start until that records a verdict. **Pre-cutover prep done 2026-09-02 evening**: Frigate rsync re-seeded (staged copy current, shrinks tomorrow's downtime delta), a stray uncommitted go-librespot config fix committed (`c52d84d`), and a real new landmine found + documented: commit `9e14f41`'s Naboo local-API fix hardcodes `host.docker.internal`, which won't resolve on woodhull's native Linux Docker -- two-line fix recorded in plan §6.2, apply right after the cutover rsync. **STT verdict recorded 2026-09-02/03 (Cowork): PASS, thin sample** -- only 9 completed voice cycles in the trial window (recorder DB has no `assist_pipeline` timing events by default; used the assist_satellite listening→processing state-gap as a proxy instead), 8/9 under the 7s target (median 4.19s), one 15.9s outlier attributed to the pre-existing Voice-PE listening-timeout bug (see item below), not the STT engine. Call-sign accuracy untested. Directional PASS, not statistically rigorous -- see plan §5.4 VERDICT for full numbers. **PHASE 2 CUTOVER EXECUTED 2026-09-02/03 evening (Cowork), same night as the STT verdict.** All four steps (mosquitto+frigate, homeassistant, Naboo, serve/monitors/watchdogs) done and verified; homeassistant/mosquitto/frigate/whisper + Naboo's two systemd units are all up and healthy on woodhull. Detector numbers matched the bake-off almost exactly (7.34ms). Full verification detail, what's confirmed vs. not, in plan doc **§6 RESULT**. Three real-world checks still need Paul (a person walking past front_window, a live voice command, Naboo on a phone) — see the three new Pending items below. **front_window check: ✅ CONFIRMED 2026-09-03 night (Paul) — person detection firing correctly in the Frigate UI at http://192.168.1.71:8971/.** Mac containers are still up (rollback intact) — **Phase 3 (stop them) not yet run.** -- (via Cowork 2026-08-30, planned 2026-08-31, Phase 1 executed 2026-08-31, pre-cutover prep 2026-09-02, STT verdict + Phase 2 cutover 2026-09-03)

- [x] **Post-cutover: verify the webhook-URL fix (go-event.py HA_URL) actually works — ✅ CONFIRMED 2026-09-03 evening (Cowork), live-tested by Paul.** `docker logs homeassistant` around the test shows BOTH the "Play HTTP stream on naboo" automation (17:36:21) and the "Stop HTTP stream on naboo" automation (17:36:30) fired correctly on real Spotify Connect state changes — the exact mechanism the 574c8d3 fix repaired. Infra side of the Naboo/Spotify Connect pipeline is now confirmed fully working end-to-end.
- [x] **✅ SOLVED 2026-09-04 (Cowork) — Naboo played nothing at all since the woodhull cutover: HA's `internal_url`/`external_url` were still `http://192.168.1.70:8123` (the dead Mac).** They live in `.storage/core.config`, so the config-dir rsync carried them across and no YAML grep could ever have found them. HA hands ESPHome media players a *proxied* URL (`/api/esphome/ffmpeg_proxy/<device>/<token>.mp3`) built from `internal_url` — so every `play_media` pointed Naboo at a dead host, silently, for Spotify and radio alike. Fixed by Paul in the UI (Settings → System → Network): internal `http://192.168.1.71:8123`, external `https://woodhull.tail317990.ts.net:8123`. Verified: KEXP held `playing` 14 s+ continuously vs. never reaching `playing` at all before; Paul's own test held 2 min 17 s. Retires two long-running false leads — "Naboo never connects to :8765" (it isn't supposed to; HA's ffmpeg proxy is the client) and the sub-second `playing` blips (those are the wake chime, which ESPHome reports as `playing`). Full writeup: TROUBLESHOOTING.md 2026-09-04 (late). — (via Cowork 2026-09-04)

- [x] **✅ DONE 2026-09-04 — Stream Deck repointed. The live file turned out to be `secrets/ha.env` (`HA_URL`), NOT `~/.streamdeck_env` as `streamdeck/CLAUDE.md` claimed — every `.sh` and `.py` button script reads ha.env; docs corrected. Verified by reading `switch.speaker` back over the new URL. Original: Stream Deck was never repointed off the dead Mac HA — `HA_URL=http://192.168.1.70:8123`** in `~/.streamdeck_env` and `streamdeck/CLAUDE.md`. `plans/HOME_AUTOMATION_STACK_MIGRATION.md` listed it as "repoint now, retire later"; it wasn't done, so every Stream Deck button that calls HA has been dead since the cutover. Same class of bug as the `internal_url` miss — the durable fix is the consumer inventory `infra/TODO.md` already argues for. — (found via Cowork 2026-09-04)

- [x] **✅ VERIFIED end-to-end 2026-09-04 10:48–10:54 ET — clean-room test, no voice interaction.** `stream.log` finally shows `CLIENT_CONNECT 172.21.0.5:51710` — the *homeassistant container's* Docker bridge address, i.e. HA's ffmpeg proxy fetching `:8765` — 129 ms before naboo reported `playing`. Then 3 min+ of unbroken `playing`, one Spotify track start to finish, 5 m 22 s of steady 25,206 bps. **The phone track-skipping was the same bug**, not a separate issue: with nothing consuming the stream, go-librespot started a track, nobody read it, and the Connect session collapsed and restarted. Also closed in this pass: `serve_http.py` THROUGHPUT counter gated on `client` (deployed to woodhull, `naboo-stream.service` restarted); the homelab meta repo cloned on woodhull so the host running HA can finally read TROUBLESHOOTING.md; and `infra`'s tracked runtime logs untracked. Full writeup: TROUBLESHOOTING.md 2026-09-04 (late). — (via Cowork 2026-09-04)

- [x] **⬇ Everything below in this cluster is SUPERSEDED by the SOLVED item above.** The whole two-day investigation — `radio_active`, the Spotify DRM restriction, the `THROUGHPUT` logging bug, "the device never fetches the stream", the self-triggered wake word, the resume-race — was chasing symptoms of HA's stale `internal_url`. Kept verbatim for the record, and worth reading: several of these are good examples of evidence that looked conclusive and wasn't. **The one item below still genuinely open is the leftover Mac `com.librespot.naboo-watchdog` LaunchAgent.** — (via Cowork 2026-09-04)

- [x] **SUPERSEDED finding #1: `radio_active` gate was a red herring** (self-cleared automatically, never blocked anything). **SUPERSEDED finding #2: Spotify DRM/`media is restricted` was real but not the primary blocker** (a clean album retest still failed the same way). Both kept for the record; full re-diagnosis below.
- [x] **SUPERSEDED finding #3: rebooting Naboo did NOT fix it — the "sustained THROUGHPUT after reboot" evidence was a false signal, traced to a real bug in `serve_http.py`'s logging.** Read the source: `_tp_bytes` (used for the `THROUGHPUT` log line) is incremented unconditionally every time `serve_http.py` reads a chunk from the ffmpeg FIFO, regardless of whether an HTTP `client` is connected — the `if client:` check only gates the `sendall()`, not the byte-counting below it. Confirmed live: `ss -tn` on woodhull showed **zero established connections to :8765** at a moment when `stream.log` was still logging steady `THROUGHPUT` growth. The real explanation: go-librespot was actively playing (Paul's phone → Spotify Connect → Naboo device target), continuously feeding ffmpeg's encoder, which counts as "throughput" even with nobody fetching the HTTP stream. **This bug should be fixed** (gate the `_tp_bytes` accounting on `if client:` too) so `THROUGHPUT` can't be misread as client activity again — not yet done.
- [x] **Test #1 done, Cowork 2026-09-04: Paul manually played a Radio Browser station (a totally different, known-good public stream, played via a totally different code path than our custom automation) directly on Naboo — it did not play either.** This rules out "something specific to our stream/URL" as the cause; `media_player.play_media` appears to be generally non-functional on this device right now, for any source.
- [x] **New finding, Cowork 2026-09-04: since the `homeassistant` container restart, Naboo's ESPHome device has produced ZERO state updates of any kind — not the media_player, not volume, not the LED ring, not the wake-word sensors, nothing — for 4+ hours, despite Paul's test.** Checked every entity under `0a3a76` in the recorder DB: nothing since the restart's own re-registration. Meanwhile every other entity in the house (cameras, sensors) is updating normally in the same window, so this isn't a broken recorder — it's specific to this device. This is different from every earlier attempt tonight, which at least produced a brief `idle→playing→idle` flicker. Something got worse, not better, after the restart.
- [x] **New finding, Cowork 2026-09-04: found a real ESPHome dual-client collision from the migration, now resolved but worth knowing about.** `docker logs homeassistant` (full history, not `--since`-filtered — see the `--since` bug noted below) shows, 2026-09-02 22:22:48 ET: `[E][voice_assistant:558]: Multiple API Clients attempting to connect to Voice Assistant — Current client: Home Assistant 2026.8.3 (192.168.1.70) — New client: Home Assistant 2026.9.0 (192.168.1.71)`. `.70` is the old Mac; `.71` is woodhull. So during cutover, Naboo briefly had both the old Mac's HA and the new woodhull HA fighting over its voice/audio subsystem. Confirmed via `docker ps -a` on the Mac that its `homeassistant` container is currently stopped (`Exited`, `unless-stopped` policy, last run ended 2026-09-03 02:39 UTC) — so this isn't an active conflict right now, but it demonstrates the device's firmware can end up in a confused state when two HA instances contend for it, which is a plausible way to explain lingering device-side weirdness that HA's own logs wouldn't show (HA only logs the *voice_assistant* conflict explicitly; a media_player-level conflict/lock might not log anything on the HA side at all).
- [x] **✅ DONE 2026-09-04 — all three Naboo LaunchAgents booted out + `launchctl disable`d on the Mac (`launchctl list | grep naboo` now empty), and the bigger half of the problem fixed too: `infra/crontab.txt` still ran `launchagent-watchdog.sh` every 5 min to *re-bootstrap* them, so re-applying that file would have put a second Spotify Connect device named "Naboo" on the network. Line commented out with the reasoning inline. `watchdog.log` (5.7 MB of failed kickstarts) truncated. Original: Related cleanup found and NOT yet done: a leftover Mac LaunchAgent (`com.librespot.naboo-watchdog`) is still running every 5 minutes right now**, logging `RESTARTING com.go-librespot.naboo — go-librespot process not found` and failing to kickstart it (`Could not find service ... in domain for user gui: 501`) because that target service was deliberately unloaded for the woodhull cutover. Currently harmless (the kickstart fails), but it's exactly the kind of process that could re-collide with woodhull's setup if `com.go-librespot.naboo` ever gets reloaded on the Mac by anything else. Belongs in the still-pending Phase 4 (Mac LaunchAgent/cron cleanup) — `launchctl unload ~/Library/LaunchAgents/com.librespot.naboo-watchdog.plist` (needs Paul, runs as his user, no sudo needed but this session doesn't have a shell on the Mac's LaunchAgent user context beyond what `mac` already provides — actually `mac` SSHes in as the right user, so this IS doable from here; just not done yet, pending confirmation this doesn't affect anything currently relied upon).
- [x] **Attempted but blocked: viewing the ESPHome device's own log page directly via browser automation** (Settings → Devices & Services → Naboo/Voice-PE → Logs), which would show the actual device-side failure instead of inferring it from silence on the HA side. The site-access approval for `woodhull.tail317990.ts.net:18123` kept expiring/denying on retry inside this session and couldn't be pushed through. **Paul, if you have 30 seconds: that Logs page is the single most valuable next piece of evidence** — screenshot or copy whatever it shows during/right after a play attempt.
- [x] **Also worth checking before going further: is Naboo's ESPHome connection even alive right now, for anything?** 4+ hours of zero telemetry is unusual even for an idle device (volume/wake-word entities usually show some noise). A quick sanity check — ask Naboo to do something simple like "turn on the light" or check if its LED ring responds to a service call from Developer Tools — would tell us if this is now a general connectivity problem (not just play_media) before spending more time on the media path specifically.
- [x] **SUPERSEDED (previous theory): the ESPHome↔HA link and webhook/automation chain were confirmed healthy in the post-reboot session, narrowing the failure to "device never fetches the stream URL."** Still true as of that test, but doesn't explain the post-restart silence above — kept for context.
- [x] **RESOLVED (root cause found), Cowork 2026-09-04: Naboo's basic HA connectivity is fully healthy (Paul confirmed light/mute/wake-sound toggles all respond instantly) — the real bug is a self-triggered wake-word loop that pauses media almost immediately after every play attempt, regardless of source.** Found from the recorder DB with millisecond precision, three independent occurrences hours apart (21:50:59, 02:59:36, 03:02:09 UTC — one from the librespot automation, one from Paul's manual Radio Browser test, one from a real voice interaction) — **every single one follows the identical pattern**: `media_player → playing`, then ~200ms later `assist_satellite → listening`, then within another ~500ms `media_player → idle`. Total window ~750-850ms every time, no exceptions. Cross-referenced against `automations.yaml`: `voice_pause_media_on_wake` (id `voice_pause_media_on_wake`) triggers on `assist_satellite` `idle → listening` and unconditionally mutes the amp, then — if media_player is `playing` — pauses it. **The moment media starts playing on Naboo, something makes its own assist_satellite immediately re-enter "listening" (a real or false wake-word detection), and that automation kills the media it just started.** This explains every symptom seen all night: "skips over and over" (repeated false-triggered pause/resume), "briefly playing then idle every time" (exactly this loop), and the Radio Browser test failing identically to Spotify (source-independent — it's triggered by assist_satellite, not by what's playing). Zero `CLIENT_CONNECT` from Naboo's IP in `serve_http.py`'s `stream.log` is also now explained: the pause can land before the ESPHome device's HTTP client finishes opening the TCP connection.
- [x] **Recommended next diagnostic test (not yet run): mute Naboo's actual microphone** (`switch.home_assistant_voice_0a3a76_mute` — confirm this is mic-mute, not amp-mute, before testing) **and retry playback.** If it plays cleanly through with the mic muted, this conclusively confirms an acoustic false-wake (the speaker's own playback start, or the mute/unmute click from `voice_pause_media_on_wake` itself, is being picked up by the mic and misheard as "Hey Nabu"). If it still cuts out with the mic muted, the false "listening" transition isn't acoustic and is coming from somewhere else in the ESPHome firmware/HA integration — a different, deeper problem.
- [x] **CORRECTED, Cowork 2026-09-04: the "self-triggered false wake word" theory above was wrong. The `assist_satellite` "listening" events are REAL voice interactions (someone saying "stop"/similar to Naboo), not acoustic false-positives — the actual bug is a race condition that silently undoes the stop.** Pulled the full `docker logs homeassistant` trail (not just the recorder DB) for both play-then-die incidents (22:59 and 23:02 ET) and both show a complete real pipeline: wake detected → ~4-5s of genuine listening/processing → **`StopMusic` intent handler actually triggered** (`Triggering intent handler <ScriptIntentHandler - StopMusic>`) → Spotify paused → **but `voice_resume_media_after_pipeline_completes` fires ~45ms later anyway and resumes playback**, which is exactly why it looked like "won't stay playing" / "won't play" — every time someone says stop, it comes back a few seconds later and then usually gets stopped again, repeat.
- [x] **Root cause of the race: `StopMusic`'s intent script (`configuration.yaml`) already tries to prevent this** — its very first action explicitly clears `input_boolean.voice_paused_music` "FIRST so the resume automation won't re-start music after the pipeline finishes" (their own comment, written by a previous session, correctly anticipating this exact bug) — **but it evidently still loses the race sometimes.** Both `StopMusic`'s intent handling and `voice_resume_media_on_done`'s trigger (`assist_satellite` → `idle`) fire off the same pipeline-completion event, as separate automations/scripts HA does not guarantee sequences between; `voice_resume_media_on_done` checks `voice_paused_music == on` immediately (before any delay), so if `StopMusic`'s clear-the-flag step hasn't landed yet at that exact instant, the resume automation reads a stale "on" and proceeds to un-pause. The existing `delay: seconds: 1` in `voice_resume_media_on_done` is AFTER the condition checks, not before — it doesn't help close this race.
- [x] **Fix candidate (not yet applied): make `voice_resume_media_on_done` re-check `voice_paused_music` right before calling `media_player.media_play`, after its existing 1s delay, instead of only checking it up front.** A second condition check after the delay would let `StopMusic`'s flag-clear (which only needs milliseconds) win the race in virtually all real-world cases, since a full second is a huge margin compared to the ~45ms race window actually observed. Low-risk, single automation edit, no device-side change needed.
- [x] **Open question, needs Paul: was the "mic muted, still won't play" retest done by voice, or via the UI/Developer Tools?** The recorder DB shows literally zero automation activity in that test window (no `assist_satellite` transitions, no `media_player.play_media`, nothing) — meaning either nothing was actually triggered (if tested by voice, a muted mic obviously can't hear a wake word at all, which would explain "won't play" for a completely different, expected reason and doesn't confirm or refute anything), or the play attempt didn't reach HA for some other reason. Needs clarifying before this test result means anything either way.
- [x] **Mic-mute test / acoustic-false-wake theory: shelved, not confirmed or denied yet** — pending the clarification above. May still be worth reattempting properly (UI-triggered play_media, mic left unmuted, just to observe without the stop-race muddying it) once the race-condition fix is in, to see if anything else is still wrong. Full writeup: TROUBLESHOOTING.md 2026-09-04 (correction). — (via Cowork 2026-09-04)
- [x] **Separate, unrelated finding from the same voice-command test: "play radio stations" via Naboo did not work.** HA logs show the `PlayCallSign` intent → `radio_play_station` script ("Play Radio Station with Fallback") started at 17:34:21 and entered a repeat/retry loop at step 2, with no further completion or error logged in the following 2 minutes — the script's own internal logic (likely a call-sign/station lookup) appears to be where it's getting stuck, not anything touched by the migration. Not yet investigated further — needs its own look at the script definition in `automations.yaml`/`scripts.yaml`. — (via Cowork 2026-09-03)
- [x] **Post-cutover: HA `400: Bad Request` on the Tailscale URL (`:18123`) — ✅ FIXED 2026-09-03 (Cowork).** `http.trusted_proxies` YAML edits had no effect — HA 2026.9 migrates `http:` config into `.storage/http` once and ignores YAML on every boot after. Fixed by editing `homeassistant/.storage/http` directly (not git-tracked, runtime state); removed the now-dead YAML `http:` block to stop a Repair-issue nag. Verified via a direct `curl -H 'X-Forwarded-For: ...'` reproduction (400 → 200), held across a container restart. See TROUBLESHOOTING.md 2026-09-03.
- [x] **Post-cutover: run one real voice command and confirm STT + the local-API bypass both fire on woodhull — ✅ CONFIRMED 2026-09-04 (Cowork).** Both proven in `homeassistant`'s own log during the 00:13 ET test: STT transcribed the command and `custom_components.spotify_voice_assistant` logged `Searching Spotify (artist) …` → `✅ SEARCH RESULT` → `▶ Started playback on Naboo via local API (bypassing Spotify cloud device list)`. So the `host.docker.internal` landmine flagged in plan §6.2 was in fact patched at cutover and resolves fine on woodhull. Audio still didn't play, but for an unrelated reason — the `internal_url` bug above. Original note: Boot-time REST snapshot showed `stt.faster_whisper` as `unavailable`, likely the known cosmetic Wyoming-entity quirk (no polling signal) rather than a real fault — a live command is the actual test. See plan §6.2 RESULT. — (via Cowork 2026-09-03)
- [x] **Post-cutover: two 2-minute manual UI cleanups — ✅ DONE by Paul, verified 2026-09-04 (Cowork).** (1) Uptime Kuma: "Frigate" monitor (id 10) hostname now `192.168.1.71`, "Home Asst" monitor (id 12) URL now `http://192.168.1.71:8123/` — both confirmed via direct DB read. **Two small residuals, not done:** the "Frigate" monitor's `url` display field is still the stale `https://192.168.1.70:8971/api/version` (cosmetic only — it's a port-type check, so hostname:port is what's actually probed, not this field); no `whisper` :10300 monitor was added. (2) HA Settings → Devices & Services: the dead `whisperkit` Wyoming integration is gone — confirmed via `core.config_entries`, only `faster-whisper` (`192.168.1.71:10300`) remains. — (via Cowork 2026-09-04)
- [x] **INCIDENT during 6.4, same-session catch+fix: Mac's `service-watchdog` LaunchAgent + `librespot/launchagent-watchdog.sh` cron revived the just-stopped Mac homeassistant/mosquitto/frigate + all three Naboo LaunchAgents ~20 min after cutover, running alongside woodhull for a window (real Naboo Spotify Connect device collision).** No data loss — caught via a routine `docker ps -a` check, fixed same session (re-stopped Mac containers, disabled both watchdogs). **Phase 3 now has a hard precondition: disable these two BEFORE the Phase 3 `docker stop`, not after.** Full incident writeup + Phase 3/4 update: plan §6.4 INCIDENT block. — (via Cowork 2026-09-03)

- [ ] **switch.plug_1 (Wyze) still `unavailable` since 08-23 07:01 — deferred by Paul.** Confirmed offline in the Wyze app (physical/Wi-Fi, not HA/DNS). `switch.speaker` had the same issue and was fixed same day (power-cycled, back `on` 08-27 23:17 UTC) — plug_1 needs the same physical power-cycle whenever Paul wants that plug back; not urgent. See TROUBLESHOOTING.md 2026-08-27. — (via Cowork 2026-08-27)

- [ ] **Frigate 0.18 is in beta (beta1–3)** — the release with per-object `max_frames`. Watch for 0.18 *stable* before upgrading; do not run beta on the load-bearing NVR. See advisor-2026-08-26.html — (via homelab-advisor 2026-08-26) — carried over (via homelab-advisor 2026-09-02): still beta (beta2/3, no stable as of 09-02). Hold.

- [ ] **Voice PE `0a3a76` esp32 crash — `Fault - IllegalInstruction` at `speaker_source_media_player:222`, +1,790 error lines in 31s.** Reflash ESPHome fw; check the media source HA hands it. See weekly-report-2026-08-16.html — (via weekly-docker-log-report 2026-08-16) — **carried over 2× (2026-08-20): 617 `[E]…:222 Media source error` in 2h on 08-19, same line. New clue — OTA is broken too (`Failed to fetch manifest` 08-20 17:29), so reflash over USB, not OTA. See weekly-report-2026-08-20.html — (via weekly-docker-log-report 2026-08-20)** — carried over 3× (via homelab-advisor 2026-09-02): media-source flood continues (1,114 `speaker_source_media_player:222` lines in the 08-31→09-02 HA log window); not reflashed. USB reflash (Paul). STALE-AND-BLOCKING.

- [ ] **front_window RTSP CSeq desync returned — the 07-22 "resolved" closure did not hold.** 6 CSeq gaps + demux timeout + 1 ffmpeg restart in 34 min (primary cam). Try `rtsp_transport: udp` + Reolink fw. See weekly-report-2026-08-16.html — (via weekly-docker-log-report 2026-08-16) — **carried over 2× but IMPROVING (2026-08-20): 29 CSeq lines in 2 clusters over 27h (08-19 20:51, 08-20 05:36) vs 6 gaps in 34 min on 08-16. Detection unaffected. Can ride to the next `-home` config change. See weekly-report-2026-08-20.html — (via weekly-docker-log-report 2026-08-20)** — (via homelab-advisor 2026-09-02): quietest yet — 1 ffmpeg restart in 2.7 days (vs 29 lines 08-20). Downgraded to low watch; rtsp_transport change still unmade.

- [x] **voice-bench harness dead since 08-07 15:42 — voice+stream data blind.** Root cause: websockets 15.x `async for` exits silently on HA's normal (1000) close + fragile library keepalive (`ping_timeout=10`). **✅ CODE FIXED 2026-08-12 (Cowork, `voice_bench.py`, untested-live): explicit `recv()`+`ConnectionClosed` reconnect, exponential backoff, `ping_timeout` 10→20, and a process-level task supervisor so no single task can silently kill the daemon. Reconnect + supervisor unit-tested green.** ⏳ **DEPLOY (Paul/host): `launchctl kickstart -k gui/$(id -u)/com.voice-bench`, then verify a NEW `voice_bench.csv` row appears on the next voice command.** ⏳ **Also host: confirm the `com.voice-bench` LaunchAgent has `KeepAlive` (it stayed dead 5 days — process-level restart may be missing) + add the "no CSV row in 48 h" ntfy liveness alert.** See advisor-2026-08-12.html. — (via homelab-advisor 2026-08-12) — **⚠️ carried over 1× (2026-08-19): harness resumed 08-16 00:44 via a MANUAL restart (8-day gap 08-07→08-16), NOT the reconnect/liveness code — no `voice_bench.py` commit since 08-07. Data flowing 08-16→08-19, but the durable fix + liveness alert are still undeployed, so it can go dark silently again. See advisor-2026-08-19.html — (via homelab-advisor 2026-08-19)** — ✅ CLOSED (via homelab-advisor 2026-09-02): verified collecting again — 19 voice_bench + 47 stream_health rows in the 08-26→09-01 window.

- [ ] 🔑 **Rotate credentials left in git history — found 2026-08-09 by the repo's first full-history `gitleaks git` scan (4 findings).** All four were real, and all four are **already remediated in the working tree** — that is why current files scan clean. The values remain in history permanently, so the fix is **rotation, not history rewriting** (one step, no risk, renders the old values worthless). Fingerprints allowlisted in `.gitleaksignore`, which documents each one as NOT a false positive.

  1. **Home Assistant long-lived access token** — was inline in `voice-bench/config.yaml` + `config.whisperkit.yaml`, now read from `./secrets/ha_token`. **Do this one first:** an HA LLAT grants full API control of the house (locks, cameras, everything) and HA long-lived tokens **do not expire**. HA → profile → Long-lived access tokens → delete the old one; confirm `secrets/ha_token` holds a genuinely new value, not the same string.
  2. **Same token class in `docker-compose.yml:192`** — the `whisper` service block; that container was removed 2026-04-22, so nothing to change in the file.
  3. **TLS private key `certs/privkey.pem`**, committed in "Initial working config" (2026-02-27). Now gitignored (`.gitignore:8`) and untracked; only the public `fullchain.pem` is tracked. Regenerate the keypair — a leaked private key lets anyone holding it impersonate the cert, and "the repo is private" is not a control.

  ⚠️ **Why this went unnoticed for 5 months:** the pre-commit hook runs `gitleaks git --staged`, which only ever sees the diff being committed. A secret in an old commit is invisible to it forever. Run `gitleaks git` (full history) on any repo periodically — a clean hook run proves nothing about history.

- [x] **~~`voice-bench/.venv` is committed to git~~ — ✅ ALREADY RESOLVED, verified 2026-09-09.** `git ls-files voice-bench/.venv` returns **0 files** — it is no longer tracked. The 22.87 MB repo-scan figure no longer applies. — (2026-08-09; closed 2026-09-09)

- [x] **Kuma monitor #20 heartbeat 300s→60s reintroduced the Voice PE errno=128 log flood — two TODO items pulled opposite ways.** The 07-29 Completed entry explicitly planned 60s→**300s** to stop it; the 08-05 instrumentation item set it **300s→60s**. Result: 1,440 HA warnings/day since 08-05 21:50. **Not the 2.4 GHz outage — still firing at exactly 60/hour as of 08-06 16:00, 19 h after the band was fixed.** **DECISION 2026-08-06 (Paul): do BOTH.** (a) ✅ `homeassistant.components.esphome.manager: error` added to the `logger:` block in `homeassistant/configuration.yaml` (Cowork) — **⏳ needs `./crestart -home` to apply**; (b) ⏳ **HOST/Paul: set Kuma monitor #20 heartbeat 60s → 180s** via the UI (192.168.1.70:3001). See weekly-report-2026-08-06.html — (via weekly-docker-log-report 2026-08-06) — **✅ CLOSED 2026-08-16: 0 `errno=128` lines in a 5.3 h HA window; commit `2de08ba` (08-06) landed. ⚠️ A different, ERROR-level esphome flood appeared and is NOT covered by that suppression — new item below. *(Move to Completed on the next tidy.)* — (via weekly-docker-log-report 2026-08-16)**

- [x] **⚠️ Verify monitor #20 still has a *proven* alert path before the 08-07 trip.** With the decision above, a Voice PE drop is invisible in the HA log and 180s-delayed in Kuma — so ntfy becomes effectively the only signal, and the infra TODO explicitly records ntfy as unproven ("do not prune Gmail before ntfy has proven itself"). Confirm `Gmail Alerts` is still attached to #20 as a second path. — (via weekly-docker-log-report 2026-08-06) — **✅ CLOSED TIME-EXPIRED 2026-08-16: the 08-07→~08-14 trip has passed. The live gap is the Kuma Gmail notifier still failing 535-5.7.8 auth — tracked in infra/TODO.md, don't duplicate here. *(Move to Completed on the next tidy.)* — (via weekly-docker-log-report 2026-08-16)**

- [ ] **`camera-monitor` can't see a dead 2.4 GHz band — it misdiagnosed the 48 h front_window outage.** Its only network probe is the *wired* gateway, so a dead 2.4 GHz band reports `gw=UP` + 100% ping loss, which reads as "camera dead" (and did, for two days — root cause was actually the band, per Paul 08-06). Add a same-band reference target so the log can say *band down* vs *device down*. See weekly-report-2026-08-06.html — (via weekly-docker-log-report 2026-08-06) — **carried over 2× (2026-08-16): no band/reference logic in `infra/scripts/camera-monitor.sh`. Low urgency — front_window `OK` on all 178 checks this week. — (via weekly-docker-log-report 2026-08-16)** — **carried over 3× (2026-08-20): still absent — the file is `infra/camera-monitor.sh` (194 lines), no band/reference probe. front_window OK 42/42. Pair with the sustained-outage item below; same file, one sitting. See weekly-report-2026-08-20.html — (via weekly-docker-log-report 2026-08-20)** — carried over 4× (via homelab-advisor 2026-09-02): unchanged. Fix in one pass with the sustained-outage alert item.

- [ ] **Add a sustained-outage alert rule for front_window.** The 48.8 h outage below ran to completion with per-sample alerts nobody acted on. Add: still DOWN after 30 min → priority-5 ntfy. See weekly-report-2026-08-06.html — (via weekly-docker-log-report 2026-08-06) — **carried over 2× (2026-08-16): no streak/sustained logic in `camera-monitor.sh`. — (via weekly-docker-log-report 2026-08-16)** — **carried over 3× (2026-08-20): still no streak logic in `infra/camera-monitor.sh`. Do it in the same edit as the band-probe item above. See weekly-report-2026-08-20.html — (via weekly-docker-log-report 2026-08-20)** — carried over 4× (via homelab-advisor 2026-09-02): the 08-29 61-min front_window outage would have tripped a 30-min rule. STALE-AND-BLOCKING (pair with the 2.4 GHz-band item).

- [x] **🔴 front_window (192.168.1.33, PRIMARY cam) DOWN ~47 h — power-cycle it. ✅ RECOVERED 2026-08-05 21:16 EDT** after `downtime=2927min` (48.8 h). camera-monitor 08-06: `OK | rtsp=UP onvif=UP gw=UP ping=5.724ms loss=0.0%`; detector-monitor `front_window det≈5–15 cap≈5.0`, inference 18–25 ms. **ROOT CAUSE (Paul 2026-08-06): the 2.4 GHz wireless network was down, now fixed — NOT a camera fault.** Retires the "gw=UP therefore camera-side" reading from the 08-05 advisor note. Same band explains the windmillac + wyzeapy errors, which stop at the same recovery. Verify it holds past the 08-07→~08-14 trip. — (verified via weekly-docker-log-report 2026-08-06) Continuous outage since 08-03 20:28 EDT (started NETWORK_WIDE, but gw=UP since 08-04 while rtsp/onvif stay DOWN, 100% ping loss = camera-side, not network). Not self-recovering like the usual 5-min FULL_REBOOT blips. Frigate now crash-loops front_window (1,124 ERR/15 min) truncating its log window. Physical power-cycle; if it stays down set enabled:false to stop the flood. See advisor-2026-08-05. — (via homelab-advisor 2026-08-05)
- [x] **Frigate backyard crash loop — ✅ DONE 2026-08-05 (via Cowork), Paul's call.** `cameras.backyard.enabled: false` is now actually written to `frigate/config.yml`. Third time this item was raised (07-23 → reopened 07-30 because the 07-27 fix never reached the file) — this time the edit is in the config, verify it stuck. Trigger was the 08-03 Velop death taking the camera's `.248` DHCP reservation with it: 858 ffmpeg ERRORs in ~20 min, truncating frigate.log's tail and hiding front_window events right before a week of travel. **Re-enable after the UX7 cutover** restores reservations — set `true`, give the camera a reserved `192.168.1.248`, `./crestart -home`. Parked until then: backyard coverage/framing, zones, Frigate+ wildlife model, WiFi-disconnect fix, recording quality. See TROUBLESHOOTING.md 2026-08-05. — (via weekly-docker-log-report 2026-07-30; closed via Cowork 2026-08-05) — **2026-08-05: crash loop currently DORMANT — backyard absent from frigate.log (camera off/normal); front_window is the active crash loop now (top item). Config fix (enabled:false) still uncommitted, so it re-triggers whenever backyard powers on during a WAN blip. — (via homelab-advisor 2026-08-05)** — **✅ FULLY CLOSED 2026-08-06: `enabled: false` is in `frigate/config.yml` and committed (`9bc1251`); last backyard ffmpeg ERROR 08-05 22:05 ET, restart 22:15, zero since. — (verified via weekly-docker-log-report 2026-08-06)**
- [ ] **stream_health logger writes bogus pipe_stall duration.** 07-25 00:19 a pipe_stall logged duration_ms=90,328,368 (~25 h), poisoning session-duration averages. Clamp/validate duration_ms at session length. Detail: advisor-2026-07-29 report. — (via homelab-advisor 2026-07-29) — **RECURRED 08-01 23:11: pipe_stall duration_ms=5,637,241 (~94 min). Confirmed, still unfixed. — (via homelab-advisor 2026-08-05)** — **2026-08-12: no recurrence in the 08-05→08-07 data (harness died 08-07, see top item); bug unfixed but did not fire. — (via homelab-advisor 2026-08-12)** — **⬆️ RECURRED 2026-08-17 19:28: pipe_stall duration_ms=153,860,906 (~42.7 h) + a session play_stop of 60,510,229 ms (~16.8 h); poisons session-duration avg. Bug fired again — move up, clamp duration_ms at session length. See advisor-2026-08-19.html — (via homelab-advisor 2026-08-19)** — carried over (via homelab-advisor 2026-09-02): recurred 09-01 19:49 (dur 8,568,520 ms ≈ 2.4 h); still unclamped, poisoning session averages.

- **Instrument the Naboo Voice PE for Wi-Fi drops (added 2026-07-22):** the 07-22 cut-out incident (TROUBLESHOOTING.md) was diagnosed blind because stock Voice PE firmware exposes no RSSI/uptime. Two parts:

  **(a) Uptime Kuma monitor — ✅ DONE 2026-08-05 (via Cowork).** Monitor id **20** `Naboo Voice PE (ESPHome API)` is live: TCP Port `192.168.1.47:6053` (retargeted from `.237` at the 2026-08-19 UX7 cutover), heartbeat **300s → 60s**, retries 2, notified via the new ntfy notifier. It already existed but was on a 5-minute heartbeat, so a drop could go unseen for five minutes. **Also wired Kuma → ntfy this session** (it had only "Gmail Alerts" — the assumption below that a `homelab` ntfy channel was already attached to Kuma was wrong). New notifier `ntfy · homelab` → `http://ntfy` (container name on the shared infra compose network — deliberately *not* `192.168.1.70:8111`, which would route through Docker Desktop's NAT and depend on the load-bearing host IP), topic `homelab`, priority 4 with DOWN events auto-escalating to 5. Test send returned "Sent Successfully"; verified in `kuma.db` that ntfy is attached to all 22 monitors. Original spec below for reference. — (closed via Cowork 2026-08-05)

  <details><summary>Original spec (kept for context)</summary>

  Add a monitor in Kuma (infra stack since 2026-07-23, no profile needed — path corrected 2026-07-30; http://192.168.1.70:3001):
  - Type: **TCP Port** (not ICMP ping — tests the actual ESPHome API HA depends on, and ESP32 ICMP is flaky)
  - Hostname **`192.168.1.47`** (a UniFi fixed IP since the 2026-08-19 cutover; it was temporarily `.237` from 2026-08-05 while the LAN had no reservations. Now that it is pinned, a red monitor means "device dead," not "lease changed"), Port `6053`
  - Heartbeat interval 60s, Retries 2, Retry interval 30s
  - Notification: existing `homelab` ntfy channel
  - Value: catches the device dropping off Wi-Fi in real time; an uptime reset shows as a monitor blip = a reboot vs. a plain Wi-Fi drop.

  </details>

  **(b) ESPHome diagnostic sensors — higher effort, needs a reflash.** Adds RSSI + uptime + which AP/band it's on. **Lower value now:** the UX7 reports per-client signal and association history in the UniFi UI, which the old gear could not. Snippet to append to the Voice PE device YAML (after the Nabu Casa package import):
  ```yaml
  sensor:
    - platform: wifi_signal
      name: "Naboo Wi-Fi Signal"
      update_interval: 60s
      entity_category: diagnostic
    - platform: uptime
      name: "Naboo Uptime"
      update_interval: 60s
      entity_category: diagnostic
  text_sensor:
    - platform: wifi_info
      bssid:
        name: "Naboo Connected AP (BSSID)"   # which UX7 radio/band it associated to
      ssid: { name: "Naboo SSID" }
      ip_address: { name: "Naboo IP" }
  ```
  **Prerequisite/decision:** HA here is a Container install (no Supervisor/add-ons), so there's no built-in ESPHome Device Builder. To flash custom YAML you must take over the device in a standalone ESPHome instance (e.g. a one-off `ghcr.io/esphome/esphome` container) using the Nabu Casa source config from `github.com/esphome/home-assistant-voice-pe`, then Install→OTA. Trade-off: once taken over, the Voice PE is no longer cloud/HA-managed for OTA updates — you own the YAML. Given that, (a) covers most of the monitoring need cheaply; only do (b) if we want RSSI/BSSID trend data.

- **Reconnect Stream Deck to the relocated server (added 2026-07-14):** the Mac mini moved upstairs (wired, static .70); the physical Stream Deck is still downstairs by the speakers/Apple TV with nothing to plug into. The deck is USB input only — buttons trigger .app bundles on the Mac which call HA/Spotify over LAN — so only USB reach is broken. Options: (1) **Stream Deck Mobile** app on a phone/iPad (~$25/yr, zero hardware, works immediately — mirrors profiles from the Elgato software over LAN); (2) **Raspberry Pi + VirtualHere USB-over-IP** downstairs (~$50 Pi + $49 license) — deck appears locally-plugged to macOS; the same Pi could host the second AdGuardHome instance from infra's DNS-failover TODO (one device, two jobs); (3) port buttons to an HA dashboard on a tablet (cleanest long-term; Claude Code project — streamdeck scripts + osacompile). USB-over-Cat5 extender ruled out unless a dedicated (non-switched) cable run between floors exists.

---

---

## 🔲 Pending  *(older / deferred)*

- [x] **✅ CLOSED 2026-09-08 — moot: the backyard camera and its whole config block are gone from `frigate/config.yml`; the camera moves to New Paltz with the M1. There is no crash loop left to reopen.** ~~⚠️ REOPENED 2026-07-30 — the config change was never made; see the new Pending item at the top. Original text below.~~ ~~**Backyard camera Frigate ffmpeg crash loop — ✅ Resolved 2026-07-27: Paul disabled the camera**~~ (`cameras.backyard.enabled: false`). Verified via frigate.log: zero backyard/Connection-refused/ffmpeg-restart lines after the restart, front_window unaffected (serving real `latest.webp` frames), HA↔Frigate `/api/stats` timeout churn gone. **NOTE: with backyard disabled, the other backyard items below (coverage/framing, zones, Frigate+ wildlife model, WiFi-disconnect fix, recording quality) are parked until the camera is re-mounted/re-enabled.** Was also the camera flooding `clips/` at `min_score 0.3` during the 2026-07-27 disk-full incident. Original: ~167 crash/restart cycles per 28-min window (`Connection refused` on rtsp://192.168.1.248), 8× HA↔Frigate timeouts. — (via weekly-docker-log-report 2026-07-23; resolved via Cowork 2026-07-27)
- [x] **Recover backyard camera (RLC-510WA, 192.168.1.248) — ✅ Verified back 2026-07-15 (homelab-advisor).** Frigate is serving live backyard streams cleanly — `/live/jsmpeg/backyard` returning 101 with multi-MB payloads and `/api/backyard/latest.webp` 200s at ~0.03s upstream response, **zero `Connection refused` in the current frigate.log window**. The Jul 9 "camera is coming back — leave enabled" decision played out: RTSP came up, the 10s ffmpeg crash loop cleared, and the HA↔Frigate stats-API timeouts it was causing are gone. Ongoing WiFi-drop robustness + the camera-monitor backyard-probe gap remain tracked separately (see the backyard-WiFi item below + infra camera-monitor item). Original context: **delivering ~0 fps since ~Jun 28, RTSP "Connection refused" since ~15:05 ET Jul 9** — detector-monitor shows `backyard cap=0.0` in ~96% of hourly checks since 2026-06-28 (183/190 samples); as of Jul 9 afternoon Frigate's ffmpeg crash-loops every ~10s on `tcp://192.168.1.248:554` Connection refused (1,328 ERROR lines in 27 min — chewing the frigate log tail). front_window unaffected (camera-monitor watches .33 only, all OK). **CORRECTION (2026-07-09, later same session): TROUBLESHOOTING 07-05 records the backyard camera was unplugged ON PURPOSE — the ~0 fps since Jun 28 is expected, not a fault.** Today's change from silent absence to active `Connection refused` at .248 suggests the camera is powered again but RTSP isn't up yet (booting/re-associating), which is what's driving the ffmpeg crash loop. Action (downgraded): if the camera is staying unplugged, set `cameras.backyard.enabled: false` in `frigate/config.yml` to stop the 10s crash loop chewing the frigate log tail; if it's being brought back, follow the staged re-enable procedure (TROUBLESHOOTING 2026-06-15) and confirm `h264Preview_01_sub` responds. **DECISION 2026-07-09 (Paul, via Cowork): camera is coming back — leave enabled in Frigate; crash loop (and the HA↔Frigate stats-API timeouts it causes) will clear once the camera's RTSP is up. Verify in next weekly report.** — (via weekly-docker-log-report 2026-07-09)
- [ ] **VAD end-of-speech / listening-timeout hangs skew voice latency (recurring 3rd week).** `listening_ms` occasionally runs away while STT stays healthy, blowing `total_ms` p95 past the 7 s target even though STT is ~1.9 s. This week (7/15–7/22, 10 cmds) two rows drove p95 to **12,683 ms**: `"Good night."` 07-22 01:00 (listening_ms **12,185**, stt 2,093) and `"I would download it all the year."` 07-15 22:32 (listening_ms **8,251**, stt 1,449) — both `music_was_playing=false`, so not amp interference. Same class flagged 07-08 (78 h runaway) and 07-15; it has now recurred three consecutive weeks. Investigate the VAD end-of-speech / `VAD_SILENCE_MS` path in `wyoming_whisperkit/handler.py` and the listening-timeout automation (`voice_listening_timeout`) — the timer isn't closing on some end-of-speech events. Claude Code task. — (via homelab-advisor 2026-07-22) — carried over 2× (homelab-advisor 2026-07-29: recurred a 4th consecutive week — 18 cmds, 100% transcribed, but listening_ms runaway drove total_ms p95 to 19,208 ms; STT healthy ~2.5 s. Worst: "Stop." listen=18,172; "Play." listen=16,470; "Goodnight." listen=17,140 — all music=false. Claude Code task) — carried over 3× (homelab-advisor 2026-08-05: 5th consecutive week; 13 cmds/100% transcribed, p95 ~16.5 s driven by 2 listening runaways — "Stop." listen=17,475, "Good night" listen=15,014, both music=false; STT healthy ~2.5 s) — carried over 4× (homelab-advisor 2026-08-12: 6th week; only 08-05→08-07 data before the harness died — "Good night" listen=12,209, "Stop." listen=11,626, both music=false; STT healthy ~2.0 s. See advisor-2026-08-12.html) — carried over 5× (homelab-advisor 2026-08-19: 7th week; 9 cmds 08-16→08-19 — "Good night" listen=11,016, music=false, STT healthy 2,049; STT avg drifted to ~2.5 s. See advisor-2026-08-19.html) — carried over 6× (homelab-advisor 2026-08-26: symptom ABSENT this week — 8 cmds, p95 5.83s under the 7s target, 0 listening-runaways; but thin volume and no code fix landed. Watch a higher-volume week before closing. See advisor-2026-08-26.html) — carried over (via homelab-advisor 2026-09-02): drove voice p95 to ~15.5 s this week (listen 9.7/16.8/12.7 s) while STT held ~1.8 s. The p95 driver.
- [ ] **stream_health correlator under-tags Mode A (pipe_stall → dropout).** On 07-15 a **233,174 ms (3.9 min) `pipe_stall`** at 22:31:56 was immediately followed by a 5-dropout cluster (22:32–22:33) and a `play_stop`, yet all those dropouts logged `correlated_stall_ms=null` → classified Mode B (AP-reconnect) when this was clearly a stall-driven cascade. The Mode A/B split is only as good as the correlation window. Widen/repair the pipe_stall→dropout association in the stream-health logger so genuine writer-stall cascades are tagged Mode A. Small logging fix; also strengthens the case for the open run-go.sh active-stall-detect work (see checked item below with open remainder). — (via homelab-advisor 2026-07-22) — carried over 2× (homelab-advisor 2026-07-29: confirmed again — 26 dropouts this week, 25 Mode B / only 1 Mode A despite 6 pipe_stall events; correlation window still misses stall→dropout cascades) — carried over 3× (homelab-advisor 2026-08-05: 16 dropouts this week, 16/16 Mode B / 0 Mode A despite 2 pipe_stall events incl. the 08-01 bogus one; still under-tags) — carried over 4× (homelab-advisor 2026-08-12: 6 dropouts 08-05→08-07 (harness died 08-07), all Mode B; 0 pipe_stall events so nothing to correlate this window — unfixed, not exercised) — carried over 5× (homelab-advisor 2026-08-26: tagged 2 Mode A this week — first non-zero in weeks — but still under-tags: the 225k/120k ms stalls on 08-19 correlated to no dropout. See advisor-2026-08-26.html) — carried over (via homelab-advisor 2026-09-02): 16/16 dropouts tagged Mode B this week despite a real 2,225 ms pipe_stall in the same minute.
- [x] **Detector inference watchdog — apple-silicon slowness goes undetected (2026-06-27 → built 2026-06-28, via Cowork)** — ✅ Built after the degradation recurred on 2026-06-28 (same ~1178 ms signature, both cameras dark; see TROUBLESHOOTING 2026-06-28). New LaunchAgent **`com.homelab.detector-monitor`** (`infra/detector-monitor.sh` + `infra/detector-monitor-launchagent/`), mirrors camera-monitor: polls Frigate `/api/stats` every 5 min for `detectors.apple-silicon.inference_speed`. **Auto-heals** when inference > 100 ms for 2 consecutive polls — kills the :5555 listener → relaunches `FrigateDetector` → `docker restart frigate` → re-polls to confirm — gated by a 30-min cooldown, logs `DEGRADED_START`/`HEAL_OK`/`HEAL_FAILED`/`COOLDOWN_SKIP`/`RECOVERY` to `infra/log-reports/detector-monitor.log`, and posts a macOS banner on heal/fail. Per-camera `detection_fps` is logged for context but deliberately NOT a heal trigger (motion-gating makes 0 legitimate — would false-heal a quiet backyard). `watchdog.sh` self-heals a stale agent hourly (the mac-monitor/camera-monitor silent-drop failure mode). Install: `cd ~/containers/infra/detector-monitor-launchagent && ./install.sh`. **Still worth checking:** whether `mediaanalysisd` (macOS ANE consumer, seen ~16:04 on 2026-06-27) is the ANE-contention trigger. — (via Cowork 2026-06-28)
- [x] **HA Wyze + Spotify DNS timeouts (api.wyzecam.com / api.spotify.com) — separate from AdGuard (2026-06-26)** — `aiodns.error.DNSError: (12, 'Timeout while contacting DNS servers')` → `ClientConnectorDNSError`, ~27 clusters Jun 24–26 (23 wyzecam, 4 spotify), still firing post-AdGuard-fix (one at 18:16 UTC Jun 26, 4 min after the AdGuard restart). **Root cause is NOT AdGuard** — HA's container sets `dns: [1.1.1.1, 8.8.8.8]` in `home-automation/docker-compose.yml` (2026-04-09 host-resolver bypass), so lookups go straight to 1.1.1.1/8.8.8.8 through Docker Desktop's resolver/NAT, which intermittently times out (UDP loss via vpnkit, likely worse under the 8 GB box's memory pressure). Plugs/AC briefly go unavailable, then recover. **Diagnosis (2026-06-26):** bursty (14 bursts over Jun 24–26, biggest 25–32 errors), hits BOTH api.wyzecam.com + api.spotify.com (→ resolver path, not the APIs), and host memory pressure was `normal` during the two largest bursts (memory ruled out). Mechanism: HA → Docker Desktop's userspace DNS forwarder (gvisor/vpnkit) → external 1.1.1.1/8.8.8.8; forwarder stalls in bursts and c-ares had no retry cushion or cache to absorb it.
**Fix applied — Option 1 (2026-06-26, Cowork):** added `dns_opt: [timeout:2, attempts:5, rotate]` to the HA compose block (hardens c-ares retries/rotation; no new dependency, preserves the 2026-04-09 independent-DNS decision). ✅ Recreated via `./crestart -home` 14:27 ET — HA init 11.16s, wyzeapi/spotify clean, zero post-restart errors. **Still open:** confirm the aiodns timeout bursts actually drop over a full week (verification window short at apply time). See TROUBLESHOOTING 2026-06-26.
**Escalation if still bursting (Option 2):** point HA primary at the now-redundant AdGuard (`192.168.1.70`, keep 8.8.8.8 secondary) for caching — but also set AdGuard `cache_ttl_min` floor (~60–300s), since current `cache_ttl_min: 0` honors short Wyze/Spotify TTLs and blunts the cache benefit. Accepts a soft HA→AdGuard dependency. **UPDATE 2026-07-01 (via homelab-advisor): Jul 1 00:51, 07:51, 14:33 UTC cluster failures CONFIRMED — all three external upstreams (1.1.1.1, 8.8.8.8, 9.9.9.10) timed out simultaneously, which means Option 1's c-ares hardening is insufficient when Docker Desktop's network layer (vpnkit/gvisor) itself fails. Escalate to Option 2: point HA dns to AdGuard (192.168.1.70) + set AdGuard cache_ttl_min 60–300s. Claude Code/Cowork task.** **UPDATE 2026-07-02 (via weekly-docker-log-report): 3rd confirmed recurrence, 17:10–17:16 ET — AdGuard 100% upstream timeout (all of 1.1.1.1/8.8.8.8/9.9.9.10/149.112.112.10/9.9.9.9), gluetun VPN cycling ("VPN server crashed"), Sonarr/Radarr/Prowlarr connection-refused to gluetun, HA Wyze/WindmillAC DNS failures — all self-healed within ~5–6 min. Still on Option 1 only. Option 2 escalation still not applied.** **UPDATE 2026-07-02 (Cowork, later same session): Paul confirmed the recurring outages line up with his ISP dropping yesterday, not a Docker-layer bug — reframes root cause but the mitigation is the same. Applied the caching half of Option 2: `infra/adguard_conf/AdGuardHome.yaml` `dns.cache_ttl_min` 0 → 300 (5 min floor on all cached records, incl. short-TTL Wyze/Spotify), edited via Cowork (no Docker access in that session to restart). **UPDATE 2026-07-03 (Cowork): Paul restarted adguardhome. Verified via `infra/log-reports/adguardhome.log` — clean restart 19:13:35 ET, `dnsproxy: cache ttl override is enabled min=300 max=0` confirms the 300s floor is live, zero errors since. Caching half of Option 2 is DONE.** **UPDATE 2026-07-03 (Cowork, same session): applied the HA-side half too — `home-automation/docker-compose.yml` homeassistant `dns:` changed from `[1.1.1.1, 8.8.8.8]` to `[192.168.1.70 (AdGuard), 8.8.8.8 fallback]`; `dns_opt` (timeout:2/attempts:5/rotate) left as-is. Accepts the soft HA→AdGuard dependency already called out in the infra reverse-proxy lessons-learned notes. Option 2 escalation is now fully applied in config. **UPDATE 2026-07-03 (Cowork, same session): Paul recreated homeassistant. Verified via `infra/log-reports/homeassistant.log` — clean restart 19:21:05 ET, wyzeapy/windmillac coordinators set up with zero errors, zero `aiodns`/`ClientConnectorDNSError`/`NameResolutionError` in ~4 min post-restart (previously immediate on every restart). Two brief self-recovering Spotify API errors (19:22/19:23 ET, no DNS traceback) look like normal post-restart auth settling, not related to this change — quiet since 19:24:35. Option 2 (both halves — AdGuard cache floor + HA routed through AdGuard) is now fully applied and verified. Real test is the next actual ISP blip — watch the next weekly report for whether Wyze/Spotify/WindmillAC survive it without the aiodns timeout signature.** **UPDATE 2026-07-09 (via weekly-docker-log-report): ✅ CLOSED.** Verified through a week containing real ISP blips: aiodns/DNS errors now appear ONLY in 4 short clusters (Jul 7 ~01Z + 18Z, Jul 8 23Z, Jul 9 17Z — 56 lines total), each coinciding with a confirmed whole-upstream outage (AdGuard simultaneously lost ALL upstream types incl. bootstrap UDP; gluetun VPN crashed inside two of the same windows) and self-healing in minutes — vs constant bursty firing before. No standalone HA-side resolver issue remains; residual errors track genuine ISP outages (tracked as an infra item). — (via weekly-docker-log-report 2026-06-26; closed 2026-07-09)
- [x] **Watch frigate memory — ✅ Resolved 2026-06-29.** The mitigation (backyard detect fps 5→3, applied 06-26) took effect: this week frigate trends **down (−7.3)**, avg **70.4%** (below the 75% threshold) and peak **93.5%** (below the 95% threshold). Settled at the expected backyard-camera baseline, no OOM/restarts. Watch condition cleared. — (via weekly-resource-budget 2026-06-29)
- [x] **Reduce backyard detect fps 5→3 (2026-06-26, via Cowork)** — ✅ Done. Changed `cameras.backyard.detect.fps: 3` in `frigate/config.yml`. Reason: Frigate memory hit 93.5% of 1536m cap (Jun 23 peak: 1436 MiB). Restart Frigate to apply: `docker compose -f ~/containers/home-automation/docker-compose.yml restart frigate`. Monitor hourly_stats.csv next week — expect avg to drop back below 80%. — (via homelab-advisor 2026-06-25)
- [x] **Re-enable backyard camera — staged procedure (2026-06-13, via Cowork)** — ✅ Done 2026-06-15. Camera re-enabled via staged procedure; verified clean WiFi reconnect, no 401s/stalls; Frigate within 1.5 GiB cap. (Procedure details preserved in TROUBLESHOOTING.md 2026-06-15.)
- [ ] ➡️ **MOVED TO NEW PALTZ 2026-09-08** — the camera leaves Brooklyn with the M1 and its config now lives in `frigate/config.np.yml`; it is out of `frigate/config.yml` entirely. This item follows the camera — track it under NP Phase 5 (`plans/NP_IMPLEMENTATION_PLAN.md`), not here. Original text: **Backyard camera coverage — too narrow (2026-06-13, via Cowork)** — Paul wants to cover a larger backyard area; 640x480 sub-stream feels "too narrow." Sort once the camera is mounted in its final position. Key facts established: resolution ≠ field of view (sub covers the exact same scene as the 5MP main, just less detail), and the RLC-510WA is a fixed 4.0mm lens, **80° H / 42° V**, **4:3 sensor** — no setting widens it and there's no 16:9 mode that adds area. Two possible fixes depending on what we see in place: (a) **out of frame** → reposition/mount higher-or-further-back, or add/swap to a wider-FOV camera (80° is standard, not wide); (b) **in frame but animals too small to detect** → tune detection: higher-res detect stream + lower `min_area`/`min_score`, and/or zones. **RAM caveat:** pushing detection toward the 5MP stream raises CPU/Neural Engine/RAM materially on the 8GB box (Frigate capped 1536m) — do a modest res bump + lower fps, not the full main stream.
- [ ] ➡️ **MOVED TO NEW PALTZ 2026-09-08** — the camera leaves Brooklyn with the M1 and its config now lives in `frigate/config.np.yml`; it is out of `frigate/config.yml` entirely. This item follows the camera — track it under NP Phase 5 (`plans/NP_IMPLEMENTATION_PLAN.md`), not here. Original text: **Draw backyard detection zones (2026-06-13, via Cowork)** — Once framing is final, draw zones in the Frigate UI and scope `review.alerts.required_zones` like `front_window` to cut noise. Tied to the coverage item above.
- [ ] ➡️ **MOVED TO NEW PALTZ 2026-09-08** — the camera leaves Brooklyn with the M1 and its config now lives in `frigate/config.np.yml`; it is out of `frigate/config.yml` entirely. This item follows the camera — track it under NP Phase 5 (`plans/NP_IMPLEMENTATION_PLAN.md`), not here. Original text: **Backyard wildlife detection — Frigate+ model (2026-06-13, via Cowork)** — Stock COCO model only detects cat/dog/bird; raccoons/possums/deer/etc. won't trigger as objects. Plan: collect snapshots → submit/label in Frigate+ (`PLUS_API_KEY` already wired) → train → swap `model:` block to the Plus model ID + add wildlife labels to `backyard.objects.track`. Verify the Plus model still includes person/car for `front_window` before swapping.
- [x] **go-librespot pipeline storm takes down Docker (2026-06-13)** — ✅ FIXED 2026-06-13 (Claude Code). Re-architected the single `go-librespot | ffmpeg | serve_http` KeepAlive pipeline into **two decoupled launchd jobs joined by a held-open FIFO** so a dropped client / playback transfer / idle can't cascade into a respawn storm: `com.go-librespot.naboo` (`run-naboo.sh`, go-librespot → FIFO, device always registered) + `com.naboo.stream` (`run-go.sh` → `serve_http.py`, which holds the FIFO open so ffmpeg never EOFs, and supervises/respawns ffmpeg in-process without exiting). Safety net: `ThrottleInterval=30` on both plists + 30s rate guards in both run-*.sh (req: a failing job can never respawn faster than once/30s). Verified host-side: jobs `runs=1`, no loop over 90s+100s windows, :8765 stays up, synthetic playback served valid MP3, writer-disconnect tore nothing down, Docker healthy throughout. **Still needs Paul's real end-to-end check** (now safe — throttled): play to Naboo, transfer to phone and back, idle; confirm no rapid restarts in `log/run.log`, audio plays. Full writeup: TROUBLESHOOTING.md 2026-06-13.
- [ ] **streamdeck librespot-monitor needs fixing or retiring (2026-06-13, NEW)** — `com.streamdeck.librespot-monitor` was DISABLED during the storm fix: its `librespot_heal.sh` does `pgrep -f "librespot" | head -1`, which in the new multi-process layout kills the wrong process (go-event/serve_http paths also match "librespot"; lowest PID wins) — it bounced the stream job. It's also built on the retired old-Rust architecture. Either fix the heal to `pgrep -f "go-librespot --config_dir"` and re-enable (`launchctl enable gui/$(id -u)/com.streamdeck.librespot-monitor`), or retire it (the new watchdog + button-script retries cover its job). Also update `streamdeck/CLAUDE.md` — stale, describes old Rust librespot/run.sh.
- [x] **Fix backyard camera WiFi disconnect pattern** — detect stream to 192.168.1.248 losing connection multiple times per night (still occurring Jul 1 19:59-20:00 ET). Camera reconnects, but detection goes dark during outages. Actions: (1) check Frigate `rtsp_transport` / retry config for backyard; (2) add backyard RTSP health to camera-monitor coverage (currently front_window only); (3) if drops persist, consider Reolink scheduled reboot to reset WiFi stack. **2026-07-22 (homelab-advisor): backyard is streaming clean with 0 `Connection refused` (log report 07-16) — the outage recovered on its own, but the preventive fix (rtsp_transport tcp + camera-monitor probe + optional Reolink reboot) was never applied, so a future WiFi drop can still go undetected. Downgraded from "blocking" to a standing preventive task.** — (via homelab-advisor 2026-06-18) — carried over 6× (STALE — Claude Code task; recovered but preventive config unapplied) — carried over 7× (homelab-advisor 2026-07-29: STALE; backyard powered off / absent in current frigate.log window = normal, no live failure; preventive config (rtsp_transport tcp + monitor probe) still unapplied. Claude Code task) — ✅ CLOSED (via homelab-advisor 2026-09-02): obsoleted by the backyard-camera retirement decision (2026-08-19); camera off/disabled since 08-05.
- [x] **Investigate front_window RTSP CSeq desync — ✅ Resolved 2026-07-22 (homelab-advisor).** — CSeq sequence errors continuing (still no improvement confirmed in Jun 25 report). Pattern not improving at 3 weeks. Try switching record stream to `rtsp_transport: udp` in `frigate/config.yml`; or check Reolink firmware update. **2026-07-15: no CSeq errors in the frigate.log window.** **2026-07-22 (homelab-advisor): RESOLVED — two consecutive clean weekly windows; weekly-docker-log-report 07-16 explicitly reports "frigate detector OK, 0 CSeq". Closing observationally; reopen if CSeq desync returns.** — (via homelab-advisor 2026-06-18) — closed 2026-07-22
- [ ] ➡️ **MOVED TO NEW PALTZ 2026-09-08** — the camera leaves Brooklyn with the M1 and its config now lives in `frigate/config.np.yml`; it is out of `frigate/config.yml` entirely. This item follows the camera — track it under NP Phase 5 (`plans/NP_IMPLEMENTATION_PLAN.md`), not here. Original text: **Backyard recording quality — 640x480 is low (2026-06-15, via Cowork)** — Backyard records off the 640x480 sub stream because WiFi at the mount can't sustain the 5MP main (record-stream Connection-timeouts + CSeq desync, ffmpeg restarting ~1x/min; fixed 2026-06-15 by dropping the main input — see frigate/config.yml backyard block + TROUBLESHOOTING context). Clips are functional but grainy for reviewing small animals across the yard. **Letting it run a few days to see if 640x480 is good enough.** If not, options: (a) lower the main-stream bitrate/resolution in the Reolink app and re-add it as the `record` input (keeps higher res, but risks the WiFi drops returning — Paul says WiFi won't improve much regardless of location); (b) wired ethernet/PoE run to the camera for a reliable full-res main stream (camera ran fine on ethernet during setup; backyard cable run may not be feasible). Detection + Frigate+ snapshots are unaffected either way (both come from the sub).
- [ ] **go-librespot migration follow-ups (2026-06-11, via Cowork)** — Cowork fixed two migration bugs same night (see TROUBLESHOOTING.md 2026-06-11): go-event.py polled nonexistent `GET /player` (→ `/status`), and watchdog.sh still targeted the old Rust binary/label. Remaining:
  - [x] **Pipeline exits on every playback transfer** — ✅ Fixed by the 2026-06-13 FIFO re-architecture above (transfer closes only the transient FIFO writer; the holder keeps ffmpeg/serve_http and the listener alive; go-librespot device registration is in its own job).
  - [x] **serve_http.py ignores SERVE_PORT env** — ✅ It already honored `SERVE_PORT`; the real cause of "Address already in use" was the `…naboo-test` plist running the same script on the same port. That plist is now retired (below).
  - [x] **Remove/disable old plists** — ✅ Retired `com.go-librespot.naboo-test.plist` and old-Rust `com.librespot.naboo.plist` (removed from repo + `~/Library/LaunchAgents/`).
  - [x] **log-tailer native coverage stale since May 5** — ✅ Fixed 2026-07-08 (Claude Code). Two-part fix: (1) `infra/log-tailer/tail.sh` now tails go-librespot log files directly from a bind-mount (`../home-automation/librespot/log:/librespot-log:ro` added to log-tailer service in `infra/docker-compose.yml`); explicit filenames only — avoids the `naboo.pcm.fifo` in the same dir. Outputs `go-librespot.log`, `go-librespot--run.log`, `go-librespot--stream.log`, `go-librespot--watchdog.log`, `go-librespot--events.log`, `go-librespot--http.log` in `infra/log-reports/`. (2) `infra/log-tailer-native/` LaunchAgent added for whisperkit/voice-bench and other native services; watchdog at `:45` in `infra/crontab.txt`. Verified live: watchdog.log showing entries at 19:40–19:50 UTC Jul 8.
  - [ ] **Verify go-event webhook fires** — on Paul's real-playback test, `events.log` should show `state stopped → playing` + `webhook sent` (HA starts the ESPHome stream + unmutes amp).
- [x] **Verify librespot survives Tuesday reboot (2026-05-12)** — ✅ Confirmed 2026-05-27. Stream sessions observed on May 20, 25, 27 — `launchctl enable` fix is working across reboots.
- [x] **Gitignore runtime log/data files (2026-06-08)** — ✅ Done 2026-06-09 (REORG Phase 0, commit `412b1ea`): `librespot/log/`, `voice-bench/log/`, `voice-bench/data/`, `wyoming-whisperkit/log/` gitignored and untracked. `automations.yaml` was clean (no pending diff).
- [x] **Naboo silent after librespot heal (2026-06-09)** — ✅ Fixed 2026-06-09, live-tested. Root cause (stream.log 2026-05-16/05-19/06-10): Voice PE abandons HTTP streams that go dry for >~few seconds; close only visible on next write (`STALL_END` → `CLIENT_DISCONNECT send_error`), leaving the player consuming-but-silent after reconnects. Fix: `serve_http.py` silence keepalive — feeds valid silent MP3 frames (`librespot/silence.mp3`) at real-time rate during stalls, capped at 45s (`SILENCE_CAP`) so an idle system still starves→idle→premutes. Silence counts toward the rate limiter, which also removed the post-stall catch-up burst. Plus: `librespot_playing` automation retries `play_media` once if Voice PE isn't playing after 3s. Watch: `SILENCE_CAP` lines in stream.log; the stop↔resilience automation ping-pong still reconnects a stopped session (now benign — plays capped silence) — tighten conditions if it bothers.
- [x] **spotify_resume restarts the song 2-3s in (2026-06-09)** — ✅ Fixed. The queue-replace used `/me/player` `progress_ms`, which lags right after playback starts (653ms reported vs ~3s heard) → `position_ms` jumped the track backward. Now uses `max(api progress, wall-clock since play succeeded)` — slight forward overshoot is inaudible, backward jumps are gone.

- [x] **Investigate Jun 28 stream dropout storm — ✅ Partial fix 2026-07-03.** Raised SILENCE_MAX_S 300→600 in `librespot/serve_http.py` (10 min cap gives 2× worst-observed stall with margin). Applied via `launchctl kickstart -k gui/$(id -u)/com.naboo.stream`; verified ffmpeg PID change in watchdog.log, :8765 still listening. **Still open:** active stall-detect → restart trigger in run-go.sh for stalls >N sec without a FIFO writer (root cause: Spotify AP reconnects dropping the writer); Jun 29 rapid restart loop root cause unknown. Claude Code task. — (via homelab-advisor 2026-07-01)
- [x] **Upgrade to HA 2026.7** — ✅ Done 2026-07-08. HACS base.py `asyncio.wait_for(timeout=30)` patch at line 620 survived; WindmillAC `blynk_service.py` `return None` patch at line 62 survived. Both verified by Cowork pre-flight check before upgrade. — (via homelab-advisor 2026-07-01)
- [x] **Review go-librespot v0.7.1 for FIFO stall improvements** — ✅ Assessed 2026-07-08 (Claude Code, research only). Conclusion: the "fail if FIFO does not have a reader" option (v0.7.0) is not applicable to our two-job architecture — `serve_http.py` holds the FIFO open permanently as the reader, so go-librespot always sees a reader and this flag would never trigger. Active stall-detect → restart trigger in `run-go.sh` (detecting writer-side FIFO stall from AP reconnects) remains the correct open task. Upgrade to v0.7.1 deferred until stall-detect task is built. — (via homelab-advisor 2026-07-08)
- [x] **Upgrade Frigate 0.17.1 → 0.17.2** — ✅ Done 2026-07-08. `ghcr.io/blakeblackshear/frigate:0.17.2-standard-arm64` confirmed on GHCR (released 2026-06-28, stable). Image tag updated in `home-automation/docker-compose.yml`; `docker compose up -d frigate` run by Paul. — (via homelab-advisor 2026-07-08)

---

---

## 🔲 Open Bugs (waiting on upstream)

- [ ] **Spotify coordinator MissingField crash loop** — HA's `spotifyaio` library fails to parse Spotify API responses since Spotify removed `GET /playlists/{id}/tracks` in Feb 2026. Error: `MissingField: Field "items" of type PlaylistTracks is missing in Playlist instance`. [GitHub #166884](https://github.com/home-assistant/core/issues/166884). HA 2026.6 shipped (Paul upgraded 2026-06-10) with **no fix** — issue still open, no milestone. Only triggers when browsing Spotify media via HA UI — not actively firing during voice commands. Watch HA 2026.7.

---

---

## 🔮 Future / Lower Priority

- [ ] **Investigate go-librespot as librespot replacement** — Go rewrite of the Spotify Connect protocol; ARM64 binary available; built-in HTTP control API (could replace serve_http.py); reports better long-term stability than C++ librespot. Goal: eliminate the heal loop, watchdog, and retry logic in spotify_resume.py.
- [ ] **Monitor small model accuracy (ongoing)** — `whisper-small-mlx-4bit` / WhisperKit small benchmarked well but needs real-world validation across voice diversity, proper nouns (WFMU, KEXP), and noisy conditions. Watch voice-bench dashboard for transcription errors.
- [ ] **voice-bench (no transcription) for radio commands** — P8/low. VAD early-trigger path produces two WhisperKit transcriptions per command; session finalizer races the slower result. Deferred — latency and hang tracking still work, transcription text unreliable for radio commands. Candidate for deprecation.
- [ ] **Frigate CoreML detection** — Apple Neural Engine path (currently using ZMQ via FrigateDetector.app)
- [ ] **HA System Monitor integration** — CPU/memory dashboard in HA
- [ ] **Centralized logging (Loki/Promtail)** — only if log-tailer + direct file reads become insufficient

---

## ✅ Completed


- **HA Voice PE (0a3a76) firmware updated 26.4.0 → 26.6.0 — ✅ DONE 2026-07-29 (Cowork, with Paul).** The recurring errno=128 flapping + esp32 crash traced to old firmware; repeated WiFi OTAs to 26.6.0 kept failing mid-download (low heap). Fixed by USB-C reflash to 26.6.0 (ESPHome 2026.6.0). One gotcha: it boot-looped while still on the laptop (`rst:0x15 USB_UART_CHIP_RESET` / `Reset Reason: USB peripheral`) — moving it to a wall USB adapter cleared it. Post-move: 25+ min stable, no resets, boot-attempt counter reset, no reconnect churn; voice verified ("What time is it?" → correct). Note HA is already on 2026.7.4. **Residual (cosmetic, not this bug):** the once-a-minute errno=128 socket-close is the uptime-kuma `Naboo Voice PE (ESPHome API)` TCP port monitor probing 192.168.1.47:6053 every 60s (bare TCP connect, no noise handshake → device logs Accept+CONNECTION_CLOSED). Harmless. Plan: drop redundant ping monitor `Voice PE (Naboo satellite)`, slow the port monitor 60s→300s (do via Kuma UI). — (via Cowork 2026-07-29)
- **serve_http silence cap raised 45s → 300s (2026-06-27, via Cowork)** — Spotify/Naboo audio kept dying while Spotify still showed "playing" and Naboo stayed selected in Connect. Root cause: go-librespot's normal Spotify AP `connection reset by peer` events (clean re-auth each time; ~19 today vs ~4 in prior 16 days, elevated by a long session + transient network roughness) produced 3–4 min FIFO stalls; `serve_http.py` `SILENCE_MAX_S` was 45s, so the silence keepalive gave up mid-stall, the `:8765` stream went dry, and the Voice PE stopped pulling → speaker silent. Bumped `SILENCE_MAX_S` 45→300 in `home-automation/librespot/serve_http.py` (graceful pause/stop unaffected — different path). Applied via `launchctl kickstart -k gui/$(id -u)/com.naboo.stream`. Verified: clean restart, cap live at 300, client reconnected, 90+s steady throughput. Full writeup: TROUBLESHOOTING.md 2026-06-27. **Watch:** AP-reset count next day — looked transient/self-healed; if it stays elevated, see the `api.spotify.com` DNS-timeout item. — (via Cowork 2026-06-27)
- **Backyard camera added (2026-06-13 → re-enabled 2026-06-15, via Cowork)** — Reolink RLC-510WA (WiFi, 5MP) for backyard wildlife. Added `backyard` block to `frigate/config.yml` (sub `h264Preview_01_sub` detect @ 640x480, main `h264Preview_01_main` record), shares `admin`/`FRIGATE_RTSP_*` creds, per-camera `objects.track: [cat, dog, bird]`. `shm_size` bumped 256m→512m for the 2nd feed. Onboarded over ethernet, then moved to WiFi: WiFi NIC has a different MAC so it took a new DHCP reservation — now at **192.168.1.248** (was .106 on ethernet). **Disabled 2026-06-13** (`enabled: false`) during the Docker Desktop crash loop (TROUBLESHOOTING 2026-06-13 — root cause was the go-librespot storm, not this camera; disabled as a headroom measure). **Re-enabled 2026-06-15** after restoring host-memory visibility (mac-monitor LaunchAgent had silently died — TROUBLESHOOTING 2026-06-15); verified reconnecting clean over WiFi from final mount, no 401s/stalls; Frigate within its 1.5 GiB cap. Post-re-enable memory settle being watched by one-off scheduled task `backyard-memory-check`. **Still open:** no zones drawn yet; wildlife detection needs a Frigate+ model (COCO only does cat/dog/bird) — see Pending.

Older completed items: `archive/TODO-completed.md`.
